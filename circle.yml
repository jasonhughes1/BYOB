machine:
  timezone:
    America/Denver
  node:
    version: 8.1.2

environment:
  DATABASE_URL: postgresql://ubuntu:@127.0.0.1:5432/curiosity_tests

database:
  override:
    - sudo -u postgres psql -p 5432 -c "CREATE DATABASE curiosity_tests;"
    - knex migrate:latest
